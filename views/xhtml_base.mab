xhtml_strict do
	head do
		title @title
		@scripts.each do | s |
			script :type => 'text/javascript', :src => s
		end if @scripts
		@styles.each do | s |
			link :href => s, :rel => 'stylesheet', :type => 'text/css'
		end if @styles
	end
	body do
		@content
	end
end